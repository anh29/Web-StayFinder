import { Box } from "@chakra-ui/react";

export interface SchedulesSectionProps {}

export default function SchedulesSection(props: SchedulesSectionProps) {
  return (
    <Box id="schedules" minH="100vh">
      Schedules Section
    </Box>
  );
}
